# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib
rmapi

# Test binary, build with `go test -c`
*.test

# Test artifacts
test_writer.zip

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Project-local glide cache, RE: https://github.com/Masterminds/glide/issues/736
.glide/
.vscode/

# Docker volume directories (ignore contents, keep directory structure)
io-backup/config/*
!io-backup/config/.gitkeep
io-backup/downloads/*
!io-backup/downloads/.gitkeep
io-backup/cache/*
!io-backup/cache/.gitkeep

